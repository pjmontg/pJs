p$.namespace("pjs.ext.core.template", function(pub, log) {
	//TODO
	pub.TemplateEngine = {
		id: 'templateEngine',
		version: '1.0',
		requires: [],
		contents: {
			//TODO search id and replace entries of object that have ${<variable_name>}
		}
	}
});
